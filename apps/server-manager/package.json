{
  "name": "knu-server-manager",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check": "tsc --noEmit && cargo test --manifest-path src-tauri/Cargo.toml",
    "stage:runtime": "node scripts/stage-runtime.mjs",
    "bundle": "npm run stage:runtime && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0"
  }
}
