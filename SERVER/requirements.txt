streamlit
psycopg[binary,pool]
pgvector
fastapi
uvicorn[standard]
python-dotenv
pydantic

httpx

# LangChain / LangGraph
# - langchain-openai: LM Studio가 OpenAI 호환 API라서 필요
# - langchain-google-genai: provider를 google로 바꿀 때 사용
langchain
langchain-core
langchain-community
langchain-google-genai
langchain-openai
langchain-postgres
langgraph
langsmith

# 임베딩 / 리랭킹
sentence-transformers
transformers
torch

# 크롤링
# - playwright: 동적 페이지 / 로그인 / JS 렌더링 처리
# - beautifulsoup4 + lxml: 정적 페이지 HTML 파싱
#
# 설치 후 로컬에서는 별도로 실행:
#   python3 -m playwright install chromium
# Dockerfile에서는 자동 실행함
playwright
beautifulsoup4
lxml

# 첨부파일 처리
# - opendataloader-pdf: PDF → 마크다운/표 추출 (ODL, Java 11+ 필요 — Dockerfile에 JRE 설치)
# - pdf2image: 스캔 PDF를 이미지로 렌더링 (VLM OCR 폴백)
# - openpyxl/xlrd: XLSX/XLS 추출
# - HWP/HWPX: LibreOffice(soffice)로 PDF 변환 후 추출 (별도 시스템 의존: soffice)
# macOS 로컬에서 pdf2image를 쓰려면:
#   brew install poppler
opendataloader-pdf
openpyxl
xlrd
pdf2image

# 이미지/OCR 처리
pillow

