{
  // VS Code/Cursor 탐색기에서 Python 캐시 파일을 숨깁니다.
  // 이유는 크롤러 관리를 편하게 하려고 폴더구조로 나눴는데, 각각의 폴더마다 파이캐시가 생성되면 보기 지저분해짐.
  // 실행에는 영향이 없고, 실제 캐시는 .gitignore로도 제외된다.
  "files.exclude": {
    "**/__pycache__": true,
    "**/*.pyc": true,
    "**/*.pyo": true
  }
}
